# 별무리 AI 문서

이 저장소는 별무리 프로젝트의 공개 AI 기술 문서를 관리한다.

- 문서를 작성하거나 수정할 때 `fluent-korean`과 `humanizer:humanizer` 스킬을 항상 읽고 적용한다.
- 과장하거나 상투적인 AI slop 문장을 쓰지 않는다. 확인하지 않은 사실도 쓰지 않는다.
- 도표는 Markdown의 Mermaid 코드로 관리하고, Astro 빌드에서 생성한 SVG를 사이트 표시의 정본으로 사용한다. React에서 Mermaid를 실행하거나 도표를 다시 생성하지 않는다.
- 배포 전 빌드 산출물에 SVG 도표가 포함되는지 확인한다. 이 파일은 웹에 공개하지 않는다.
