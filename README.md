# 별무리 AI 문서

Astro와 Starlight로 만든 공개 문서 사이트다. 공개할 Markdown의 정본은 `src/content/docs/`에 둔다.

```bash
npm install
npm run dev
npm run build
npm run preview
```

GitHub Pages는 `main`에 반영된 버전을 기존 개인 도메인의
`https://pysunn.me/docs-beolmuri-ai/`에 배포한다.
